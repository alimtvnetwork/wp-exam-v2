import React from 'react';
import { FormBuilder } from '@/components/forms/FormBuilder';

/**
 * QuizEditor - Modernized wrapper for WP Exam Question & Form Builder.
 * Supports Quizzes, Employee Sign-up forms, Surveys, and Sequential progression.
 */
export const QuizEditor: React.FC = () => {
  return <FormBuilder />;
};

export default QuizEditor;
