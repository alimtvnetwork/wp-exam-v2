# Absolute Paths Ban

Verbatim quote from user: "when you are writing anything to the file system, why you have to write from file, file? You just use slash and from the root of the repo, it would be automatically fixed. Make sure you follow this rule in the future because the, the code needs to be... everything needs to be standalone, not from a file system."
