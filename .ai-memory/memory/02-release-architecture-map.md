---
name: Release Architecture Map
description: Architectural map of how versioning and releases work in this codebase (where version lives, how it propagates, sync pipeline, and release ceremony).
type: standard
---

# Release Architecture Map

**Repository:** alimtvnetwork/coding-guidelines-v24
**Canonical Version Source:** package.json (master version) + version.json (root single source of truth)
**Reading Queue:** Enqueued in .ai-memory/what-to-read.md and linked in
eadme.md

## 1. Overview of Release Architecture

This repository uses an automated synchronization and release pipeline where version changes propagate deterministically across all manifests, documentation, stamps, health scores, and mirrors.

`
                  ┌─────────────────┐
                  │  package.json   │  (Master Version)
                  └────────┬────────┘
                           │
                 scripts/sync-version.mjs
                           │
                  ┌────────▼────────┐
                  │  version.json   │  (Root Single Source of Truth)
                  └────────┬────────┘
                           │
       ┌───────────────────┼───────────────────┐
       │                   │                   │
scripts/sync-spec-tree  scripts/sync-health-score  scripts/sync-readme-stats
       │                   │                   │
┌──────▼────────┐   ┌──────▼──────────┐ ┌──────▼────────┐
│ specTree.json │   │health-score.json│ │readme.md badge│
└───────────────┘   └─────────────────┘ └───────────────┘
`

## 2. Where the Version Lives

1. **package.json**:
   Contains "version": "X.Y.Z". This is the canonical master version read by standard npm tooling and the release bump script (scripts/bump-version.mjs).

2. **version.json**:
   The runtime Single Source of Truth at repository root. Generated by scripts/sync-version.mjs. It embeds:
   - Self-explaining metadata (_purpose, _instructions)
   - Version: The global repository version
   - Backend: "inherit" (inherits global version)
   - Frontend: "inherit" (inherits global version)
   - linters: "3.79.0" (dedicated toolchain track)
   - Authors: Author attribution details
   - Prompts: List of active AI prompts

3. **Pin Sites (Lock-Step Update)**:
   -
eadme.md: Header shields badge (img.shields.io/badge/version-X.Y.Z-...), stats line (vX.Y.Z).
   - docs/architecture.md, docs/principles.md, docs/author.md: <!-- STAMP:VERSION -->X.Y.Z<!-- /STAMP:VERSION -->.
   - public/health-score.json: "version": "X.Y.Z".
   - src/data/specTree.json: Generated navigation tree with version metadata.
   - prompt-version.template.json & version.template.json: Blueprints for downstream repos.

## 3. How Version Propagates (Sync Pipeline)

Running
pm run sync triggers five core synchronization scripts in sequence:

1. **scripts/sync-version.mjs**:
   Reads package.json, version-authors.json, and scripts/prompt-sync-config.json. Emits root version.json.
2. **scripts/sync-spec-tree.mjs**:
   Walks spec/ markdown files, extracts titles/versions from frontmatter, emits src/data/specTree.json.
3. **scripts/sync-health-score.mjs**:
   Computes spec completeness, rule coverage, and audit health score, emits public/health-score.json.
4. **scripts/sync-readme-stats.mjs**:
   Stamps versions, folder counts, file counts, and update timestamps into
eadme.md and docs/*.md.
5. **scripts/sync-guidelines.mjs**:
   Mirrors 02-spec/17-consolidated-guidelines/34-compiled-simple-coding-guidelines.md to .cursorrules and .ai-memory/coding-guidelines.md.

## 4. Release Ceremony Flow

A complete release follows these strict steps:

1. **Pre-flight**:
   - Check git status (clean state required).
   - Run git pull to ensure local branch is in sync with upstream.
   - Verify version bump tier (e.g. MINOR: 6.32.0 -> 6.32.0).
2. **Bumping**:
   - Run
ode scripts/release.mjs --tier minor --scope "<headline>" --skip-slides.
   - Updates package.json, runs
pm run sync, prepends changelog.md and 02-spec/19-main-worker-service/98-changelog.md.
3. **Drift Verification**:
   - Run
ode scripts/sync-check.mjs --fix to verify 0 drift across all 7 sync-managed files.
4. **Commit, Tag & Push**:
   - git commit -m "release: vX.Y.Z <headline>"
   - git tag vX.Y.Z
   - git push && git push origin vX.Y.Z

## 5. Bundle Installer Integration

When downstream repositories run any bundle installer (*-install.sh or *-install.ps1):
- version.json is smart-merged into the target's version.json under codingGuideline.
- .ai-memory/memory/ and .ai-memory/what-to-read.md are copied into the target repo, transferring this architectural knowledge directly.

## 5. AI Rules for Versioning (STRICTLY AVOID)

🔴 **AI AGENTS MUST NEVER MANUALLY MODIFY VERSION INFORMATION OR DATES.**

- **Do NOT** modify `version.json` manually to bump a version.
- **Do NOT** modify `version.json` manually to change an `updated` or `job date`.
- **Do NOT** modify `package.json` version fields manually.
- **Do NOT** modify `changelog.md` dates manually.

**Why:** The version information and dates are managed strictly by synchronization repositories and the automated `release.mjs` / `sync.mjs` pipeline. Manual edits by AI cause drift, pipeline failures, and merge conflicts.

**How to find Repository Information:**
If an AI agent needs to check or verify repository information, it must examine the `.git` folder (e.g., using `git remote -v` or viewing `.git/config`). Do NOT assume `version.json` is safe for manual editing just because you need repository details.
